/**
 * Core utilities module
 * 
 * This module provides:
 * - UTC date/time utilities (getUTCDayNumber, getNextUTCMidnight, etc.)
 * - UTC dateKey utilities (getUTCDateKey) for solve counter
 * - Configuration (API paths, environment variables)
 */

export * from './date'
export * from './utc-date'
export * from './config'

